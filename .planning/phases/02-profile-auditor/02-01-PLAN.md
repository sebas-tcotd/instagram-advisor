---
phase: 02-profile-auditor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/entities/AuditResult.ts
  - src/infrastructure/ai/GeminiProvider.ts
  - src/infrastructure/ai/AnthropicProvider.ts
  - src/infrastructure/ai/GeminiProvider.test.ts
  - src/infrastructure/ai/AnthropicProvider.test.ts
  - src/application/AuditProfile.test.ts
autonomous: true
requirements:
  - PROF-01
  - PROF-03
must_haves:
  truths:
    - "Both GeminiProvider.auditProfile() and AnthropicProvider.auditProfile() return a typed AuditResult (no 'not implemented' throws)"
    - "AuditResult contains overallScore (number), status (string), checklist (ChecklistItem[]), and wins (string[])"
    - "validateAuditResult() rejects invalid priority values and unparseable overallScore strings"
    - "pnpm test passes all tests including the new auditProfile describe blocks"
  artifacts:
    - path: src/domain/entities/AuditResult.ts
      provides: AuditResult + ChecklistItem + Priority types
      contains: "wins: string[]"
    - path: src/infrastructure/ai/GeminiProvider.ts
      provides: callGeminiText() helper + validateAuditResult() + auditProfile() implementation
      contains: "auditProfile"
    - path: src/infrastructure/ai/AnthropicProvider.ts
      provides: validateAuditResult() + auditProfile() text-only implementation
      contains: "auditProfile"
    - path: src/application/AuditProfile.test.ts
      provides: use case delegation tests (3 cases)
      contains: "AuditProfile"
  key_links:
    - from: src/infrastructure/ai/GeminiProvider.ts
      to: src/domain/entities/AuditResult.ts
      via: "import type { AuditResult, ChecklistItem }"
      pattern: "import type.*AuditResult.*ChecklistItem"
    - from: src/infrastructure/ai/AnthropicProvider.ts
      to: src/domain/entities/AuditResult.ts
      via: "import type { AuditResult, ChecklistItem }"
      pattern: "import type.*AuditResult.*ChecklistItem"
---

## Phase Goal

**As a** developer, **I want to** run `npm run profile` and open a Profile tab in the web UI, **so that** I get a structured audit of my Instagram profile with an overall score, strengths, improvement areas, and concrete recommendations.

<objective>
Update the AuditResult domain entity to match the real prompt schema, implement auditProfile() in both providers (replacing the Phase 1 stubs), and add complete test coverage. After this plan, the profile-auditor agent backend is fully operational — providers return a valid AuditResult on any call instead of throwing.

Purpose: The entity and providers are the shared foundation for both CLI (Plan 02) and UI (Plan 03). Both downstream plans depend on this work being correct before they can ship anything real.

Output: Updated AuditResult.ts entity, auditProfile() implementations in both providers, AuditProfile.test.ts, and updated provider test files — all green under pnpm test.
</objective>

<execution_context>
@/Users/tcotd/seb-lab/instagram-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tcotd/seb-lab/instagram-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tcotd/seb-lab/instagram-advisor/.planning/PROJECT.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/ROADMAP.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/STATE.md

@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/02-profile-auditor/02-CONTEXT.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/02-profile-auditor/02-RESEARCH.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/02-profile-auditor/02-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update AuditResult entity and create AuditProfile use case tests</name>
  <files>src/domain/entities/AuditResult.ts, src/application/AuditProfile.test.ts</files>
  <read_first>
    - src/domain/entities/AuditResult.ts — current Phase 1 placeholder (lines 1-9); read before replacing
    - src/application/AuditProfile.ts — verify execute() signature is execute(profileYaml: string): Promise&lt;AuditResult&gt; (D-13: signature stays unchanged)
    - src/domain/entities/PostAnalysisResult.ts — pattern for co-located union types with the result interface
    - prompts/profile-auditor.md — the actual AI response schema; this is ground truth for the entity shape
    - src/infrastructure/ai/AnthropicProvider.test.ts — use-case delegation test style (mock via vi.fn())
  </read_first>
  <behavior>
    - AuditResult has fields: overallScore (number), status (string), checklist (ChecklistItem[]), wins (string[])
    - ChecklistItem has fields: priority (Priority union), element (string), issue (string), action (string)
    - Priority is 'urgente' | 'importante' | 'mejora'
    - ChecklistItem and Priority are co-located in AuditResult.ts and exported as named types
    - AuditProfile.execute() delegates to provider.auditProfile() with the given profileYaml string unchanged
    - AuditProfile.execute() returns the provider result unchanged (no transformation)
    - AuditProfile.execute() propagates provider errors without wrapping
  </behavior>
  <action>
    Replace the entire content of src/domain/entities/AuditResult.ts with the new schema per D-01, D-02, D-03. The new file exports: type Priority = 'urgente' | 'importante' | 'mejora'; interface ChecklistItem with fields priority, element, issue, action; interface AuditResult with fields overallScore (number, parsed from "X/10" by providers per D-02), status (string, 1-2 line summary per D-03), checklist (ChecklistItem[]), wins (string[], strengths per PROF-03 requirement). The wins field is a top-level array, NOT inside ChecklistItem. Add a JSDoc comment on wins explaining it satisfies PROF-03 "at least one strength".

    Create src/application/AuditProfile.test.ts with three test cases following the vi.fn() mock pattern from the PATTERNS.md analog: (1) calls provider.auditProfile() once with the exact profileYaml string; (2) returns the AuditResult from the provider without modification (use toBe for reference equality); (3) propagates provider Error without wrapping (rejects.toThrow('API failure')). The mock provider is typed as unknown as AIProvider so TypeScript is satisfied without implementing all methods.
  </action>
  <verify>
    <automated>cd /Users/tcotd/seb-lab/instagram-advisor && pnpm test src/application/AuditProfile.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/domain/entities/AuditResult.ts exports Priority, ChecklistItem, and AuditResult with the wins field present
    - src/application/AuditProfile.test.ts has 3 passing tests
    - pnpm typecheck exits 0 (no TS errors from the entity change)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement auditProfile() in GeminiProvider and AnthropicProvider</name>
  <files>
    src/infrastructure/ai/GeminiProvider.ts,
    src/infrastructure/ai/AnthropicProvider.ts,
    src/infrastructure/ai/GeminiProvider.test.ts,
    src/infrastructure/ai/AnthropicProvider.test.ts
  </files>
  <read_first>
    - src/infrastructure/ai/GeminiProvider.ts — full file; identify: callGemini() helper structure (lines ~48-118) for the callGeminiText() variant; validatePostAnalysisResult() pattern (lines ~14-32); current auditProfile stub (line ~154)
    - src/infrastructure/ai/AnthropicProvider.ts — full file; identify: analyzePost() pattern (lines ~68-88); extractJSON() function (line ~47); current auditProfile stub (line ~113)
    - src/infrastructure/ai/promptUtils.ts — verify assembleSystemPrompt() signature; confirms D-11 (accepts 'profile-auditor.md' and config)
    - src/infrastructure/ai/GeminiProvider.test.ts — identify makeGeminiResponse(), makeConfig(), fetchSpy helpers; identify existing auditProfile stub test block to replace (lines ~145-151)
    - src/infrastructure/ai/AnthropicProvider.test.ts — identify makeAnthropicResponse(), makeConfig(), mockCreate helpers; identify existing auditProfile stub test block to replace (lines ~130-136)
    - src/domain/entities/AuditResult.ts — read the updated entity from Task 1 before writing import statements
  </read_first>
  <behavior>
    GeminiProvider tests:
    - auditProfile with valid JSON returns AuditResult with overallScore=7, status string, checklist[0].priority='urgente', wins[0] present
    - auditProfile with missing status/checklist throws error containing 'missing required AuditResult fields'
    - auditProfile with invalid priority ('critico') throws error containing 'checklist item missing required fields'
    - auditProfile with unparseable overall ('siete') throws error containing 'Cannot parse overallScore'

    AnthropicProvider tests (same 4 cases as Gemini plus):
    - sends text-only content — mockCreate called with messages[0].content = [{ type: 'text', text: 'profile yaml content' }] (no image block per D-12)

    GeminiProvider.auditProfile() behavior:
    - calls assembleSystemPrompt('profile-auditor.md', this.config) for the system prompt (D-11)
    - calls callGeminiText() (new text-only variant — no inlineData in request body per D-12)
    - passes result to validateAuditResult() which parses 'overall' string to overallScore via parseInt("X/10".split('/')[0], 10) (D-02)
    - returns typed AuditResult

    AnthropicProvider.auditProfile() behavior:
    - calls assembleSystemPrompt('profile-auditor.md', this.config) for the system prompt (D-11)
    - calls this.client.messages.create() with content: [{ type: 'text', text: profileYaml }] — no image block (D-12)
    - passes raw text through extractJSON() then validateAuditResult()
    - returns typed AuditResult
  </behavior>
  <action>
    In GeminiProvider.ts: (a) Add callGeminiText(apiKey, model, systemPrompt, userText, maxTokens) function after callGemini(). Copy the full fetch + error handling + JSON extraction logic from callGemini() unchanged (lines ~70-118). The only difference: contents[0].parts contains only [{ text: userText }] with no inlineData item. (b) Add validateAuditResult(parsed: unknown): AuditResult function following validatePostAnalysisResult() pattern. Check obj['overall'] is string, obj['status'] is string, obj['checklist'] is Array. Loop through checklist items checking element/issue/action are strings and priority is in VALID_PRIORITIES const (['urgente','importante','mejora']). Parse overallScore via parseInt(str.split('/')[0], 10) and throw if isNaN. Return AuditResult object with wins: Array.isArray(obj['wins']) ? obj['wins'] : []. (c) Replace the auditProfile stub with the real async method that calls assembleSystemPrompt('profile-auditor.md', this.config), callGeminiText with config fields, validateAuditResult. Add import type { AuditResult, ChecklistItem } from '../../domain/entities/AuditResult' (ChecklistItem needed for the loop type in validateAuditResult).

    In AnthropicProvider.ts: (a) Add the identical validateAuditResult() function (copy from GeminiProvider, change error prefix to 'Anthropic response missing required AuditResult fields'). (b) Replace the auditProfile stub with the real async method: assembleSystemPrompt, messages.create with text-only content array, extractJSON(raw), validateAuditResult. Add import type { AuditResult, ChecklistItem } from '../../domain/entities/AuditResult'.

    In GeminiProvider.test.ts: Replace the existing auditProfile stub test block (the one that tests 'throws Phase 2 message') with the 4 test cases described in the behavior block above. Use the existing makeGeminiResponse() and fetchSpy helpers.

    In AnthropicProvider.test.ts: Replace the existing auditProfile stub test block with 5 test cases (the 4 from Gemini pattern plus the text-only content assertion). Use mockCreate and makeAnthropicResponse() helpers.
  </action>
  <verify>
    <automated>cd /Users/tcotd/seb-lab/instagram-advisor && pnpm test 2>&1 | tail -30</automated>
  </verify>
  <done>
    - pnpm test exits 0 with all suites passing (including new auditProfile describe blocks in both provider test files)
    - GeminiProvider.ts contains callGeminiText function with no inlineData in the request body
    - AnthropicProvider.ts auditProfile() passes content: [{ type: 'text', text: profileYaml }] to messages.create
    - pnpm typecheck exits 0
    - pnpm lint exits 0 (no @typescript-eslint violations)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI response → domain entity | Unstructured JSON from external AI API crosses into typed domain layer via validateAuditResult() |
| profile.yaml → system prompt | Static committed file; not user-controlled at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | validateAuditResult() in GeminiProvider + AnthropicProvider | mitigate | Validate all required fields before constructing AuditResult; priority validated against VALID_PRIORITIES const array — no arbitrary string accepted |
| T-02-02 | Tampering | checklist[].priority field in AI response | mitigate | VALID_PRIORITIES const = ['urgente','importante','mejora']; any other value throws Error before reaching application layer |
| T-02-03 | Tampering | overall score string in AI response | mitigate | parseInt("X/10".split('/')[0]) with isNaN guard; malformed strings throw Error |
| T-02-04 | Information Disclosure | profile.yaml inlined in system prompt | accept | Static committed file; personal tool with no PII beyond the owner's own Instagram identity data |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- pnpm test exits 0 (all test files including new AuditProfile.test.ts and updated provider test files)
- pnpm typecheck exits 0
- pnpm lint exits 0
- src/domain/entities/AuditResult.ts exports Priority, ChecklistItem, AuditResult with wins: string[] field
- GeminiProvider.ts has no 'not implemented' string in auditProfile() method
- AnthropicProvider.ts has no 'not implemented' string in auditProfile() method
</verification>

<success_criteria>
Both provider stubs replaced with real implementations; the entire test suite is green; AuditResult entity has the correct schema including wins[]; pnpm typecheck and pnpm lint both exit 0. Plans 02 and 03 can proceed in parallel because the domain type and provider logic are complete.
</success_criteria>

<output>
Create `.planning/phases/02-profile-auditor/02-01-SUMMARY.md` when done.
</output>
