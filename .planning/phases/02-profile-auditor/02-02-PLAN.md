---
phase: 02-profile-auditor
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/cli/profile.ts
  - package.json
autonomous: true
requirements:
  - PROF-01
  - PROF-03
must_haves:
  truths:
    - "npm run profile executes without error and prints a structured audit to the terminal"
    - "Output shows overall score, status summary, checklist items grouped by priority (urgente/importante/mejora), and wins list"
    - "Score color is RED for <4, YELLOW for 4-6, GREEN for >=7"
  artifacts:
    - path: src/cli/profile.ts
      provides: Zero-argument CLI entry point for profile auditor
      contains: "AuditProfile"
    - path: package.json
      provides: npm run profile script
      contains: "profile"
  key_links:
    - from: src/cli/profile.ts
      to: src/infrastructure/ai/AIProviderFactory.ts
      via: "createAIProvider()"
      pattern: "createAIProvider"
    - from: src/cli/profile.ts
      to: src/application/AuditProfile.ts
      via: "new AuditProfile(provider)"
      pattern: "new AuditProfile"
---

<objective>
Create src/cli/profile.ts as a zero-argument CLI entry point that reads profile.yaml from the repo root, calls the AuditProfile use case, and prints a priority-grouped ANSI-colored audit to the terminal. Add "profile" script to package.json. After this plan, `npm run profile` is a working command that satisfies PROF-01.

Purpose: Delivers the first user-visible deliverable for Phase 2 — a developer can run the profile auditor from the terminal and see a real structured response.

Output: src/cli/profile.ts (new), package.json (updated with profile script).
</objective>

<execution_context>
@/Users/tcotd/seb-lab/instagram-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tcotd/seb-lab/instagram-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tcotd/seb-lab/instagram-advisor/.planning/PROJECT.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/ROADMAP.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/STATE.md

@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/02-profile-auditor/02-CONTEXT.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/02-profile-auditor/02-PATTERNS.md

@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/02-profile-auditor/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/cli/profile.ts and add npm run profile script</name>
  <files>src/cli/profile.ts, package.json</files>
  <read_first>
    - src/cli/analyze.ts — read the full file; this is the exact structural analog (imports, ANSI constants, top-level await, try/catch pattern, error exit with process.exit(1))
    - src/infrastructure/config/loadConfig.ts — verify the loadConfig() return type includes profile_path field (needed to resolve profile.yaml path per D-04)
    - src/application/AuditProfile.ts — verify execute(profileYaml: string) signature (D-13: unchanged from Phase 1 scaffold)
    - src/domain/entities/AuditResult.ts — read the updated entity from Plan 01 to know ChecklistItem.priority values for grouping logic
    - package.json — read current scripts block before adding the profile entry; also confirm "tsx" runner is used for analyze and caption scripts
  </read_first>
  <action>
    Create src/cli/profile.ts as a zero-argument CLI script following the exact structure of analyze.ts. Key implementation specifics (per D-04, D-05):

    Static imports at top (no dynamic imports — per analyze.ts pattern and RESEARCH.md pitfall 5): import 'dotenv/config'; import { readFileSync } from 'fs'; import { resolve } from 'path'; import { createAIProvider } from '../infrastructure/ai/AIProviderFactory'; import { AuditProfile } from '../application/AuditProfile'; import { loadConfig } from '../infrastructure/config/loadConfig';

    ANSI constants (copy verbatim from analyze.ts): BOLD, DIM, GREEN, YELLOW, RED, RESET.

    Header print: `\n${BOLD}profile-auditor${RESET} ${DIM}@sebas_tcotd${RESET}` then `${DIM}Auditando perfil...${RESET}\n`

    Core execution in try/catch: (a) const config = loadConfig(); (b) const profileYaml = readFileSync(resolve(process.cwd(), config.profile_path), 'utf8'); (c) const provider = createAIProvider(); (d) const useCase = new AuditProfile(provider); (e) const result = await useCase.execute(profileYaml);

    Score output: const scoreColor = result.overallScore >= 7 ? GREEN : result.overallScore >= 4 ? YELLOW : RED; Print `${scoreColor}${BOLD}Puntuación general: ${result.overallScore}/10${RESET}` then `${DIM}${result.status}${RESET}\n`

    Priority grouping: filter checklist into urgentes/importantes/mejoras arrays. For each non-empty group, print a section header (urgentes: `${RED}${BOLD}— URGENTE —${RESET}`, importantes: `${YELLOW}${BOLD}— IMPORTANTE —${RESET}`, mejoras: `${DIM}${BOLD}— MEJORA —${RESET}`). For each item: `  ${colorConst}${i.element}${RESET}: ${i.issue}\n  ${DIM}→ ${i.action}${RESET}\n`

    Wins section (if result.wins?.length): `${GREEN}${BOLD}Bien hecho:${RESET}` then each win as `  ${GREEN}✓${RESET} ${w}` then blank line.

    Catch block: `console.error(\`\n${RED}Error: ${e instanceof Error ? e.message : String(e)}${RESET}\n\`); process.exit(1);`

    In package.json scripts block, add "profile": "tsx src/cli/profile.ts" following the exact same pattern as the "analyze" and "caption" entries.
  </action>
  <verify>
    <automated>cd /Users/tcotd/seb-lab/instagram-advisor && pnpm typecheck 2>&1 | tail -10 && pnpm lint 2>&1 | tail -10</automated>
  </verify>
  <done>
    - src/cli/profile.ts exists and pnpm typecheck exits 0
    - pnpm lint exits 0 (no @typescript-eslint violations in the new file)
    - package.json scripts block contains "profile": "tsx src/cli/profile.ts"
    - grep -c 'createAIProvider' src/cli/profile.ts returns 1
    - grep -c 'new AuditProfile' src/cli/profile.ts returns 1
    - No dynamic import() statements in src/cli/profile.ts
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>src/cli/profile.ts CLI entry point + npm run profile script in package.json. The profile-auditor agent runs from the terminal, reads profile.yaml from the repo root, calls the AuditProfile use case via the configured AI provider, and prints a priority-grouped ANSI audit to stdout.</what-built>
  <how-to-verify>
    1. From the repo root, run: `npm run profile`
    2. Confirm the command runs without error (exit code 0)
    3. Verify the output includes:
       - "Puntuación general: X/10" line with a color (green/yellow/red depending on score)
       - A status summary line in dim color
       - At least one priority section header (— URGENTE —, — IMPORTANTE —, or — MEJORA —)
       - At least one checklist item showing element: issue → action format
    4. If the configured provider is Gemini, the VITE_GEMINI_API_KEY env var must be set in .env. If Anthropic, ANTHROPIC_API_KEY must be set.
    5. Optional: open .env and temporarily change ai.provider in config.yaml to the other provider, re-run, confirm it still works (tests PROV-05 from Phase 1).
  </how-to-verify>
  <resume-signal>Type "approved" if output matches, or describe any issues seen</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile.yaml → CLI → AI provider | Static committed file read via fs.readFileSync; not user-supplied at runtime |
| AI response → terminal output | Validated AuditResult from provider; CLI only formats it as ANSI strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | src/cli/profile.ts | accept | API key accessed via process.env (loaded by dotenv); key value never printed to stdout — only presence checked by doctor.ts |
| T-02-06 | Tampering | profile.yaml path resolution | accept | Path sourced from config.profile_path (trusted config file at repo root, not user-supplied); resolve(process.cwd(), config.profile_path) is safe for a personal tool |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- src/cli/profile.ts exists
- package.json contains "profile": "tsx src/cli/profile.ts"
- pnpm typecheck exits 0
- pnpm lint exits 0
- npm run profile runs without error and outputs a structured audit
- Human checkpoint approved
</verification>

<success_criteria>
`npm run profile` produces a structured, ANSI-colored audit result showing overall score, status, grouped checklist items, and wins list. PROF-01 is satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-profile-auditor/02-02-SUMMARY.md` when done.
</output>
