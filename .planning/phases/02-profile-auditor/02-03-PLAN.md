---
phase: 02-profile-auditor
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/ui/pages/ProfilePage.tsx
  - src/ui/components/PriorityBadge.tsx
  - src/ui/hooks/useAIProvider.ts
  - src/ui/App.tsx
autonomous: false
requirements:
  - PROF-02
  - PROF-03
must_haves:
  truths:
    - "The web UI has a Profile tab accessible from the tab navigation bar"
    - "Clicking the Profile tab shows a left-panel summary of key profile.yaml fields (handle, name, bio, niche)"
    - "The Audit my profile button triggers the AI call and shows a loading spinner"
    - "The result shows overall score via ScoreBar, status text, priority-grouped checklist items with PriorityBadge, and wins list"
    - "Profile tab is always accessible regardless of whether an image has been uploaded"
  artifacts:
    - path: src/ui/pages/ProfilePage.tsx
      provides: Profile page component with controls + results
      contains: "useAuditProfile"
    - path: src/ui/components/PriorityBadge.tsx
      provides: Priority badge component for urgente/importante/mejora labels
      contains: "PriorityBadge"
    - path: src/ui/hooks/useAIProvider.ts
      provides: useAuditProfile hook + callGeminiText helper
      contains: "useAuditProfile"
    - path: src/ui/App.tsx
      provides: Extended Tab union and Profile tab render
      contains: "'profile'"
  key_links:
    - from: src/ui/App.tsx
      to: src/ui/pages/ProfilePage.tsx
      via: "tab === 'profile' && <ProfilePage />"
      pattern: "tab.*profile.*ProfilePage"
    - from: src/ui/pages/ProfilePage.tsx
      to: src/ui/hooks/useAIProvider.ts
      via: "useAuditProfile()"
      pattern: "useAuditProfile"
    - from: src/ui/pages/ProfilePage.tsx
      to: src/ui/components/ScoreBar.tsx
      via: "ScoreBar label='puntuación general' score={`${result.overallScore}/10`}"
      pattern: "overallScore.*10"
---

<objective>
Add the Profile tab to the web UI: create ProfilePage.tsx with left-panel YAML summary + "Audit my profile" button and right-panel result rendering; create PriorityBadge.tsx component; add useAuditProfile hook and callGeminiText helper to useAIProvider.ts; extend the Tab union and tab list in App.tsx. After this plan, a user can open the browser, click the Profile tab, and receive a structured audit displayed with ScoreBar + priority-grouped checklist items.

Purpose: Delivers the UI half of PROF-02. Parallel to Plan 02 (CLI) — both can execute simultaneously after Plan 01 completes.

Output: ProfilePage.tsx (new), PriorityBadge.tsx (new), useAIProvider.ts (updated with useAuditProfile export + callGeminiText), App.tsx (updated with 'profile' tab).
</objective>

<execution_context>
@/Users/tcotd/seb-lab/instagram-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tcotd/seb-lab/instagram-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tcotd/seb-lab/instagram-advisor/.planning/PROJECT.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/ROADMAP.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/STATE.md

@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/02-profile-auditor/02-CONTEXT.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/02-profile-auditor/02-PATTERNS.md

@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/02-profile-auditor/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PriorityBadge component, callGeminiText helper, and useAuditProfile hook</name>
  <files>src/ui/components/PriorityBadge.tsx, src/ui/hooks/useAIProvider.ts</files>
  <read_first>
    - src/ui/components/VerdictBadge.tsx — full file; read every line; PriorityBadge is a direct structural copy with priority-specific color mappings replacing verdict colors
    - src/ui/hooks/useAIProvider.ts — full file; read every line before editing: identify callGemini() function body (lines ~35-107) to copy for callGeminiText(); identify buildSystemPrompt() (lines ~22-24); confirm profileRaw is already imported as @root/profile.yaml?raw; identify useGenerateCaption() (lines ~151-183) as the hook pattern to follow for useAuditProfile()
    - src/domain/entities/AuditResult.ts — read updated entity from Plan 01 for correct field names in the hook's result construction
    - vite.config.ts — confirm @root and @prompts alias definitions (needed for ProfilePage.tsx imports in Task 2)
  </read_first>
  <action>
    Create src/ui/components/PriorityBadge.tsx following the exact VerdictBadge.tsx structure (per PATTERNS.md and CONTEXT.md Claude's Discretion note). Import type Priority from '../../domain/entities/AuditResult'. Define Props interface with priority: Priority. The color map (Record&lt;Priority, { label, color, bg }&gt;) values: urgente → { label: 'urgente', color: 'var(--red)', bg: 'var(--red-bg)' }; importante → { label: 'importante', color: 'var(--yellow)', bg: 'var(--yellow-bg)' }; mejora → { label: 'mejora', color: 'var(--text-3)', bg: 'var(--bg-2)' }. Return &lt;span&gt; with the same inline style as VerdictBadge (display: 'inline-block', padding: '3px 10px', borderRadius: 'var(--radius)', fontSize: '11px', letterSpacing: '0.06em', color, background, border).

    In src/ui/hooks/useAIProvider.ts, make the following additions (do NOT modify any existing exports or functions):

    (a) Add imports after the existing imports: import profileAuditorPrompt from '@prompts/profile-auditor.md?raw' and import type { AuditResult } from '../../domain/entities/AuditResult'. Note: profileRaw is already imported — do not re-import it.

    (b) Add callGeminiText(systemPrompt: string, userText: string): Promise&lt;unknown&gt; function after the existing callGemini() function. Copy the entire fetch + error handling + JSON extraction block from callGemini() verbatim. The only structural difference: the request body's contents[0].parts array is [{ text: userText }] only — no inlineData element. Keep the same API_KEY check at the top, same maxTokens variable usage, same code-fence stripping + JSON.parse with /\{[\s\S]*\}/ fallback pattern.

    (c) Add useAuditProfile() export after useGenerateCaption() following the exact same [loading, result, error] state triple + run() pattern. The run() function: calls callGeminiText(buildSystemPrompt(profileAuditorPrompt), profileRaw); receives raw parsed JSON (the AI returns 'overall' not 'overallScore'); constructs the AuditResult by parsing overallScore via parseInt((obj['overall'] as string).split('/')[0], 10), extracting status, checklist, and wins (default to [] if absent); sets result via setResult(). The returned object: { loading, result, error, run }.
  </action>
  <verify>
    <automated>cd /Users/tcotd/seb-lab/instagram-advisor && pnpm typecheck 2>&1 | tail -15</automated>
  </verify>
  <done>
    - src/ui/components/PriorityBadge.tsx exports function PriorityBadge({ priority })
    - src/ui/hooks/useAIProvider.ts exports useAuditProfile function
    - src/ui/hooks/useAIProvider.ts contains callGeminiText function with no inlineData in request body
    - pnpm typecheck exits 0
    - Existing exports (useAnalyzePost, useGenerateCaption) are unchanged (verify by checking their test behavior in pnpm test)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ProfilePage.tsx and extend App.tsx with Profile tab</name>
  <files>src/ui/pages/ProfilePage.tsx, src/ui/App.tsx</files>
  <read_first>
    - src/ui/pages/AnalyzePage.tsx — full file; read every line; ProfilePage follows this exact structure: styles object pattern, error/loading/result render order (error first, then loading spinner, then result), section divs, button pattern
    - src/ui/App.tsx — read the full file before editing; identify: Tab type line (~line 37), tab array in the map call (~line 79), page component render conditionals (~lines 124-125), ProfilePage import location
    - src/ui/components/ScoreBar.tsx — read the full file to confirm the score prop accepts a string in "X/10" format (the regex is /^(\d+)\s*\/\s*(\d+)$/); this confirms D-09 must use score={`${result.overallScore}/10`} NOT score={result.overallScore * 10}
    - src/domain/entities/AuditResult.ts — read updated entity for ChecklistItem fields and Priority union to use in the checklist render loop
    - profile.yaml — read the top-level fields (identity.username, identity.name, bio_target or similar) to know what to display in the left-panel summary (D-07)
  </read_first>
  <action>
    Create src/ui/pages/ProfilePage.tsx. ProfilePage receives no props — it is self-contained (D-07 reads profile.yaml via ?raw import; D-06 specifies Vite ?raw at build time).

    Imports: import { useAuditProfile } from '../hooks/useAIProvider'; import { ScoreBar } from '../components/ScoreBar'; import { PriorityBadge } from '../components/PriorityBadge'; import { Spinner } from '../components/Spinner'; import type { ChecklistItem } from '../../domain/entities/AuditResult'; import profileYaml from '@root/profile.yaml?raw'; import { load } from 'js-yaml'.

    Parse the YAML in the component body (outside the return, before the JSX): const profile = load(profileYaml) as Record&lt;string, unknown&gt;. Extract the identity sub-object and bio fields. If the profile YAML structure uses an 'identity' key with username/name sub-keys, access them via (profile['identity'] as Record&lt;string, unknown&gt;)['username']. Display gracefully if fields are undefined.

    Styles object (const s = {...}): follow the same shape as AnalyzePage.tsx's s object. Add profile-specific styles: profileSummary (background: 'var(--bg-2)', border: '1px solid var(--border)', borderRadius: 'var(--radius)', padding: '12px 14px', marginBottom: 16), statusText (fontSize: 12, color: 'var(--text-3)', margin: '8px 0 16px'), priorityGroup (marginBottom: 16), checklistItem (marginBottom: 12, paddingLeft: 8, borderLeft: '2px solid var(--border)').

    Left panel content (before the run button): A profileSummary div showing: handle (@username), name, and bio excerpt (first 80 chars). Labels above each field use the label style (font-size: 10px, uppercase, text-3 color). This satisfies D-07.

    Run button: disabled={loading}. onClick calls void run(). Button text: loading ? &lt;&gt;&lt;Spinner /&gt; auditando...&lt;/&gt; : '→ auditar perfil'. The button is never disabled based on image state (per RESEARCH.md pitfall 3 — ProfilePage is self-contained).

    Results section (after the button): render in this order: (1) error div if error exists (red bg box with "ERROR" label); (2) loading spinner div if loading; (3) result content if result && !loading.

    Result content per D-08, D-09, D-10: ScoreBar with label="puntuación general" and score={`${result.overallScore}/10`} (string form — critical per RESEARCH.md pitfall 2 and ScoreBar API); status paragraph; priority groups rendered by mapping over ['urgente', 'importante', 'mejora'] as const — filter checklist items for each priority, skip if empty, otherwise show PriorityBadge header + items list where each item shows element (bold), issue text, and action text (prefixed with → in dim style); wins section if result.wins?.length (green ✓ prefix per each win).

    In src/ui/App.tsx: (a) Add import { ProfilePage } from './pages/ProfilePage' alongside the other page imports. (b) Extend Tab type: type Tab = 'analyze' | 'caption' | 'profile'. (c) Add ['profile', 'perfil'] entry to the tab array in the map call. (d) Add {tab === 'profile' && &lt;ProfilePage /&gt;} after the caption conditional. Do NOT wrap ProfilePage in any image null check.
  </action>
  <verify>
    <automated>cd /Users/tcotd/seb-lab/instagram-advisor && pnpm build 2>&1 | tail -15</automated>
  </verify>
  <done>
    - pnpm build exits 0 (Vite bundles successfully with ?raw imports and js-yaml)
    - pnpm typecheck exits 0
    - src/ui/App.tsx Tab type includes 'profile'
    - src/ui/App.tsx contains ProfilePage render that is NOT gated on image state
    - ScoreBar in ProfilePage.tsx receives score as template literal string "X/10" not overallScore * 10
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Profile tab in the web UI: ProfilePage with left-panel YAML summary, "Audit my profile" button, and right-panel result showing ScoreBar, status text, priority-grouped checklist with PriorityBadge components, and wins list. App.tsx updated with 'profile' tab.</what-built>
  <how-to-verify>
    1. Run: `npm run dev` and open http://localhost:5173 in the browser
    2. Confirm three tabs appear in the tab bar: "analizar post", "generar caption", "perfil"
    3. Click the "perfil" tab — verify it is accessible WITHOUT uploading an image first
    4. Verify the left panel shows a profile summary with handle/name/bio excerpt from profile.yaml
    5. Click "→ auditar perfil" — verify a loading spinner appears
    6. After the AI responds, verify the right panel shows:
       - A score bar labeled "puntuación general" with a non-zero fill (not 0%)
       - A status text paragraph below the score bar
       - At least one priority group with a colored badge (urgente/importante/mejora)
       - Each checklist item shows element name, issue description, and action (with → prefix)
       - Wins section appears with ✓ prefix items (if the AI response includes wins)
    7. Verify no console errors in the browser dev tools
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any rendering issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Gemini REST API | API key (VITE_GEMINI_API_KEY) inlined in Vite bundle at build time — accepted known risk |
| Gemini JSON response → React state | Raw AI response parsed by callGeminiText(); overallScore validated via parseInt + isNaN guard in hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | useAIProvider.ts / callGeminiText | accept | VITE_GEMINI_API_KEY inlined in bundle — accepted per Phase 1 architecture decision; personal tool, dist/ gitignored |
| T-02-08 | Tampering | useAuditProfile hook — AI response parsing | mitigate | overallScore parsed via parseInt with isNaN guard; checklist rendered by React mapping (no innerHTML, no dangerouslySetInnerHTML) |
| T-02-09 | Information Disclosure | profile.yaml ?raw import | accept | profile.yaml baked into Vite bundle — same risk as VITE_GEMINI_API_KEY; personal tool, no server-side exposure |
| T-02-10 | Tampering | profile.yaml → YAML parse in browser | accept | js-yaml.load() on a static committed file with no user-controlled input; no code injection vector |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan (js-yaml already in package.json) |
</threat_model>

<verification>
- pnpm build exits 0
- pnpm typecheck exits 0
- Three tabs visible in browser UI: analizar post, generar caption, perfil
- Profile tab accessible without image upload
- Score bar renders with non-zero fill after AI responds
- Human checkpoint approved
</verification>

<success_criteria>
The web UI Profile tab is fully functional: user can audit their Instagram profile from the browser without uploading an image, view a structured result with ScoreBar, priority-grouped checklist items with PriorityBadge, and wins list. PROF-02 and PROF-03 are satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-profile-auditor/02-03-SUMMARY.md` when done.
</output>
