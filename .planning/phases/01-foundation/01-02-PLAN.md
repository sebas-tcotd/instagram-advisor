---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - src/domain/entities/PostAnalysisResult.ts
  - src/domain/entities/CaptionResult.ts
  - src/domain/entities/AnalyzeRequest.ts
  - src/domain/entities/CaptionRequest.ts
  - src/domain/entities/AuditResult.ts
  - src/domain/ports/AIProvider.ts
autonomous: true
requirements:
  - MIGR-04
  - AGNT-01
  - AGNT-02
  - AGNT-03

must_haves:
  truths:
    - "src/domain/entities/ contains five typed interfaces: PostAnalysisResult, CaptionResult, AnalyzeRequest, CaptionRequest, AuditResult"
    - "src/domain/ports/AIProvider.ts defines the interface with three separate typed methods (D-05)"
    - "domain layer has zero imports from infrastructure, ui, or cli"
    - "pnpm typecheck exits 0 after domain files are added"
  artifacts:
    - path: "src/domain/ports/AIProvider.ts"
      provides: "Central architectural contract — AIProvider interface"
      exports: ["AIProvider"]
    - path: "src/domain/entities/PostAnalysisResult.ts"
      provides: "Typed shape of post analysis AI response"
      exports: ["PostAnalysisResult", "Verdict", "Scores"]
    - path: "src/domain/entities/CaptionResult.ts"
      provides: "Typed shape of caption generation AI response"
      exports: ["CaptionResult", "Caption"]
    - path: "src/domain/entities/AnalyzeRequest.ts"
      provides: "Typed input to post analysis use case"
      exports: ["AnalyzeRequest"]
    - path: "src/domain/entities/CaptionRequest.ts"
      provides: "Typed input to caption generation use case"
      exports: ["CaptionRequest"]
    - path: "src/domain/entities/AuditResult.ts"
      provides: "Typed shape for profile audit (scaffold — full implementation in Phase 2)"
      exports: ["AuditResult"]
  key_links:
    - from: "src/domain/ports/AIProvider.ts"
      to: "src/domain/entities/"
      via: "import type statements"
      pattern: "import type.*from.*entities"
---

<objective>
Define the authoritative TypeScript contracts for the entire project: entity shapes and the AIProvider port interface.

Purpose: These files are the foundation of the Clean Architecture. Application and infrastructure layers depend on them. They must be finalized before any provider or use case can be written. This plan completes MIGR-04 (ports as pure TS interfaces) and establishes the domain contracts required by AGNT-01/02/03.

Output: Six TypeScript files in src/domain/ — five entity types and the AIProvider port interface. No runtime code in domain. No imports from infrastructure, ui, or cli.
</objective>

<execution_context>
@/Users/tcotd/seb-lab/instagram-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tcotd/seb-lab/instagram-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tcotd/seb-lab/instagram-advisor/.planning/ROADMAP.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/REQUIREMENTS.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/01-foundation/01-CONTEXT.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/01-foundation/01-RESEARCH.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/01-foundation/01-PATTERNS.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create domain entity type files</name>
  <files>
    src/domain/entities/PostAnalysisResult.ts,
    src/domain/entities/CaptionResult.ts,
    src/domain/entities/AnalyzeRequest.ts,
    src/domain/entities/CaptionRequest.ts,
    src/domain/entities/AuditResult.ts
  </files>

  <read_first>
    - scripts/analyze.js — lines 9-14 (arg shapes → AnalyzeRequest) and lines 87-98 (result rendering → PostAnalysisResult shape)
    - scripts/caption.js — lines 9-11 (arg shapes → CaptionRequest) and lines 77-84 (result rendering → CaptionResult shape)
    - .planning/phases/01-foundation/01-PATTERNS.md — entity sections: PostAnalysisResult.ts, CaptionResult.ts, AnalyzeRequest.ts, CaptionRequest.ts pattern assignments (contains exact TypeScript interfaces to produce)
    - .planning/phases/01-foundation/01-RESEARCH.md — Code Examples section (PostAnalysisResult interface), Domain Entity Example
  </read_first>

  <action>
  Create src/domain/entities/ directory. Create five TypeScript files.

  PostAnalysisResult.ts — derives from scripts/analyze.js result rendering (lines 87-98):
    - Export type Verdict = 'listo' | 'ajustar' | 'no va'
    - Export interface Scores { visual: string; caption: string; fit: string }  (values like "8/10")
    - Export interface PostAnalysisResult { verdict: Verdict; scores: Scores; analysis: string; suggestions: string[] }

  CaptionResult.ts — derives from scripts/caption.js result rendering (lines 77-84):
    - Export interface Caption { tone: string; hook_type: string; text: string }
    - Export interface CaptionResult { captions: Caption[]; notes?: string }

  AnalyzeRequest.ts — derives from scripts/analyze.js CLI args (lines 9-14) and App.jsx state:
    - Export interface AnalyzeRequest { imageBase64: string; mimeType: 'image/jpeg' | 'image/png' | 'image/webp'; format: 'post_individual' | 'carrusel' | 'historia' | 'reel'; layer: 'externa' | 'interna' | 'engineer'; caption?: string }

  CaptionRequest.ts — derives from scripts/caption.js CLI args (lines 9-11):
    - Export interface CaptionRequest { imageBase64: string; mimeType: 'image/jpeg' | 'image/png' | 'image/webp'; tone: 'narrativo' | 'introspectivo' | 'sensorial' | 'proceso' | 'tensión' }

  AuditResult.ts — scaffold for Phase 2, minimal shape that satisfies the AIProvider interface:
    - Export interface AuditResult { overallScore: number; strengths: string[]; improvements: string[]; recommendations: string[] }
    - Add a JSDoc comment: // Phase 1 scaffold — full implementation in Phase 2 (PROF-01/PROF-02/PROF-03)

  Style rules (CLAUDE.md + PATTERNS.md conventions):
    - Single quotes for string literals
    - No semicolons at end of interface member lines? No — TypeScript interfaces use semicolons by convention in this project
    - 2-space indentation
    - PascalCase for interfaces and types
    - Export each interface/type individually (no barrel index.ts in domain/entities/)

  CRITICAL (MIGR-03): These files must contain ONLY TypeScript type/interface/type alias declarations. No imports from infrastructure/, ui/, or cli/. No runtime code. ESLint boundaries will enforce this, but check manually before finishing.
  </action>

  <verify>
    <automated>cd /Users/tcotd/seb-lab/instagram-advisor && pnpm typecheck 2>&1 | grep -E "error|Error" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - src/domain/entities/ directory exists with exactly five .ts files
    - PostAnalysisResult.ts exports Verdict type, Scores interface, PostAnalysisResult interface
    - CaptionResult.ts exports Caption interface and CaptionResult interface
    - AnalyzeRequest.ts exports AnalyzeRequest interface with imageBase64, mimeType, format, layer, caption? fields
    - CaptionRequest.ts exports CaptionRequest interface with imageBase64, mimeType, tone fields
    - AuditResult.ts exports AuditResult interface with overallScore, strengths, improvements, recommendations fields
    - None of the five files contain any import from '../infrastructure', '../ui', or '../cli'
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0 (boundaries rule passes for all domain entity files)
  </acceptance_criteria>

  <done>Five domain entity files exist; all are pure TypeScript interfaces with no layer boundary violations; typecheck and lint pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create AIProvider port interface</name>
  <files>src/domain/ports/AIProvider.ts</files>

  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md — D-05 (separate typed methods per use case, no generics, maximum readability), D-04 (interfaces as authoritative source of truth)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 1 (AIProvider interface — exact shape)
    - .planning/phases/01-foundation/01-PATTERNS.md — AIProvider.ts pattern assignment (greenfield, exact interface to produce)
    - src/domain/entities/ — the five files just created (imports will come from here)
  </read_first>

  <action>
  Create src/domain/ports/ directory. Create AIProvider.ts.

  This file is the central architectural showcase of the entire phase. Per D-05 (locked): separate typed methods per use case, no generics, no discriminated unions. Any developer who opens this file cold must understand the entire AI contract in under 30 seconds.

  AIProvider.ts content:
    - Import type AnalyzeRequest from '../entities/AnalyzeRequest'
    - Import type CaptionRequest from '../entities/CaptionRequest'
    - Import type PostAnalysisResult from '../entities/PostAnalysisResult'
    - Import type CaptionResult from '../entities/CaptionResult'
    - Import type AuditResult from '../entities/AuditResult'
    - Export interface AIProvider with three methods:
        analyzePost(req: AnalyzeRequest): Promise<PostAnalysisResult>
        generateCaption(req: CaptionRequest): Promise<CaptionResult>
        auditProfile(profileYaml: string): Promise<AuditResult>

  Add a file-level JSDoc comment block explaining the port pattern to a developer reading for the first time:
    // AIProvider — the port (interface) that isolates the application layer from
    // any specific AI provider. Implement this interface in src/infrastructure/ai/
    // to add a new provider. Change which implementation is used via config.yaml.

  Style: single quotes, 2-space indent. CRITICAL: all three imports must use 'import type' (not plain 'import') — this keeps the domain layer as pure type declarations with no runtime module graph.

  CRITICAL (MIGR-03): This file imports only from ../entities/ (still within domain). Zero imports from infrastructure, ui, or cli. ESLint boundaries must pass.
  </action>

  <verify>
    <automated>cd /Users/tcotd/seb-lab/instagram-advisor && pnpm typecheck && pnpm lint 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - src/domain/ports/AIProvider.ts exists
    - AIProvider.ts exports interface AIProvider with exactly three methods: analyzePost, generateCaption, auditProfile
    - analyzePost signature: (req: AnalyzeRequest) => Promise<PostAnalysisResult>
    - generateCaption signature: (req: CaptionRequest) => Promise<CaptionResult>
    - auditProfile signature: (profileYaml: string) => Promise<AuditResult>
    - All five entity imports use 'import type' syntax
    - None of the imports reference infrastructure/, ui/, or cli/
    - File contains the explanatory JSDoc comment about the port pattern
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0 (boundaries: domain imports domain only)
  </acceptance_criteria>

  <done>AIProvider.ts defines the three-method interface with separate typed methods (D-05); all imports are 'import type'; typecheck and lint pass; the file is self-explanatory for a developer reading it cold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain/entities/ → domain/ports/ | Internal domain boundary — entities flow up to ports; no external boundaries crossed here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Domain layer importing infrastructure | mitigate | eslint-plugin-boundaries with 'error' severity blocks any cross-layer import at lint time (D-03); pnpm lint is run as acceptance criteria for both tasks |
| T-02-02 | Information Disclosure | AuditResult scaffold exposing partial profile data shape | accept | Phase 1 scaffold contains only field names (overallScore, strengths, etc.), no actual data; no sensitivity concern |
| T-02-SC | Tampering | npm/pnpm installs | mitigate | No new packages installed in this plan; all deps from Plan 01-01 already approved |
</threat_model>

<verification>
After both tasks complete:

1. `pnpm typecheck` exits 0 — all six domain files are valid TypeScript
2. `pnpm lint` exits 0 — no boundary violations in domain layer
3. `ls src/domain/entities/` shows five .ts files: PostAnalysisResult.ts, CaptionResult.ts, AnalyzeRequest.ts, CaptionRequest.ts, AuditResult.ts
4. `ls src/domain/ports/` shows AIProvider.ts
5. `grep -r 'from.*infrastructure\|from.*ui\|from.*cli' src/domain/` returns empty — zero cross-layer imports
</verification>

<success_criteria>
- Six TypeScript files in src/domain/ (five entities + one port)
- AIProvider interface has three separate typed methods (D-05 — no generics)
- All domain files use 'import type' for cross-file imports
- Zero imports from infrastructure, ui, or cli in any domain file (MIGR-03)
- pnpm typecheck and pnpm lint both pass
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
