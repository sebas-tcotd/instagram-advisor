---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/application/AnalyzePost.ts
  - src/application/GenerateCaption.ts
  - src/application/AuditProfile.ts
  - src/application/AnalyzePost.test.ts
  - src/application/GenerateCaption.test.ts
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - AGNT-03

must_haves:
  truths:
    - "AnalyzePost use case lives in src/application/ and has no direct knowledge of GeminiProvider or AnthropicProvider"
    - "GenerateCaption use case lives in src/application/ and has no direct knowledge of any provider"
    - "AuditProfile use case is scaffolded in src/application/ (Phase 2 will implement the body)"
    - "Application use case tests pass with mocked AIProvider — no real API calls"
  artifacts:
    - path: "src/application/AnalyzePost.ts"
      provides: "Post analysis orchestration use case"
      exports: ["AnalyzePost"]
    - path: "src/application/GenerateCaption.ts"
      provides: "Caption generation orchestration use case"
      exports: ["GenerateCaption"]
    - path: "src/application/AuditProfile.ts"
      provides: "Profile audit use case scaffold"
      exports: ["AuditProfile"]
    - path: "src/application/AnalyzePost.test.ts"
      provides: "Unit tests for AnalyzePost use case"
      contains: "AnalyzePost"
  key_links:
    - from: "src/application/AnalyzePost.ts"
      to: "src/domain/ports/AIProvider.ts"
      via: "constructor injection"
      pattern: "constructor.*AIProvider"
    - from: "src/application/GenerateCaption.ts"
      to: "src/domain/ports/AIProvider.ts"
      via: "constructor injection"
      pattern: "constructor.*AIProvider"
---

<objective>
Implement the application use cases that orchestrate AI calls via the AIProvider port.
These classes are the heart of Clean Architecture: they know WHAT to do (call the provider) but not HOW (which provider or which SDK).

Purpose: Completes AGNT-01, AGNT-02, AGNT-03. The use cases are what the CLI and UI layers call — they act as the single interface between presentation and infrastructure. Constructor injection means no use case ever imports GeminiProvider or AnthropicProvider directly.

Note: Plan 04 runs in parallel with Plan 03 (Wave 2). Application use cases depend only on domain entities/ports (Plan 02), not on infrastructure providers (Plan 03). The dependency graph allows parallel execution.

Output: Three application use case files + unit tests for AnalyzePost and GenerateCaption.
</objective>

<execution_context>
@/Users/tcotd/seb-lab/instagram-advisor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tcotd/seb-lab/instagram-advisor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tcotd/seb-lab/instagram-advisor/.planning/ROADMAP.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/REQUIREMENTS.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/01-foundation/01-CONTEXT.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/01-foundation/01-RESEARCH.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/01-foundation/01-PATTERNS.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/01-foundation/01-01-SUMMARY.md
@/Users/tcotd/seb-lab/instagram-advisor/.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AnalyzePost and GenerateCaption use cases with unit tests</name>
  <files>
    src/application/AnalyzePost.ts,
    src/application/GenerateCaption.ts,
    src/application/AnalyzePost.test.ts,
    src/application/GenerateCaption.test.ts
  </files>

  <read_first>
    - src/domain/ports/AIProvider.ts — the interface the use cases depend on (constructor type)
    - src/domain/entities/AnalyzeRequest.ts — input type for AnalyzePost.execute()
    - src/domain/entities/CaptionRequest.ts — input type for GenerateCaption.execute()
    - src/domain/entities/PostAnalysisResult.ts — return type for AnalyzePost.execute()
    - src/domain/entities/CaptionResult.ts — return type for GenerateCaption.execute()
    - .planning/phases/01-foundation/01-PATTERNS.md — AnalyzePost.ts and GenerateCaption.ts pattern assignments (contains exact class code to produce)
    - .planning/phases/01-foundation/01-CONTEXT.md — AGNT-01/AGNT-02 requirements and constructor injection pattern
  </read_first>

  <behavior>
    - new AnalyzePost(provider) constructs a use case with the provider injected
    - AnalyzePost.execute(req) calls provider.analyzePost(req) and returns the result
    - AnalyzePost.execute(req) propagates any Error thrown by the provider (no catching or swallowing)
    - new GenerateCaption(provider) constructs a use case with the provider injected
    - GenerateCaption.execute(req) calls provider.generateCaption(req) and returns the result
    - Both use cases are pure orchestrators: no business logic, no provider knowledge, no SDK imports
  </behavior>

  <action>
  TDD approach — write the test first, then the implementation.

  Create src/application/AnalyzePost.ts (per RESEARCH.md Pattern 2 and PATTERNS.md AnalyzePost.ts section):
    - Import type AIProvider from '../domain/ports/AIProvider'
    - Import type AnalyzeRequest from '../domain/entities/AnalyzeRequest'
    - Import type PostAnalysisResult from '../domain/entities/PostAnalysisResult'
    - Export class AnalyzePost
    - Constructor: constructor(private readonly provider: AIProvider) {}
    - Method: async execute(req: AnalyzeRequest): Promise<PostAnalysisResult> { return this.provider.analyzePost(req) }
    - The class body is intentionally minimal — the complexity lives in the provider

  Create src/application/GenerateCaption.ts — identical structure to AnalyzePost but with CaptionRequest/CaptionResult types and calls provider.generateCaption(req).

  Create src/application/AnalyzePost.test.ts:
    - Create a mock AIProvider using vi.fn() for each method
    - Test: execute(req) calls provider.analyzePost exactly once with the same req object
    - Test: execute(req) returns the resolved value from provider.analyzePost
    - Test: execute(req) rejects with the same Error when provider.analyzePost rejects

  Create src/application/GenerateCaption.test.ts — same structure testing generateCaption method.

  CRITICAL import rule (AGNT-01/MIGR-03): AnalyzePost.ts and GenerateCaption.ts must ONLY import from '../domain/'. Under no circumstances should these files import from '../infrastructure/', '../ui/', or '../cli/'. The ESLint boundaries rule will enforce this at lint time.

  Style: single quotes, 2-space indent, semicolons present.
  All domain imports use 'import type' (not plain 'import') — same convention as domain files.
  </action>

  <verify>
    <automated>cd /Users/tcotd/seb-lab/instagram-advisor && pnpm vitest run src/application/ --reporter=verbose 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - src/application/AnalyzePost.ts exports class AnalyzePost with execute() method
    - src/application/GenerateCaption.ts exports class GenerateCaption with execute() method
    - Both files contain "private readonly provider: AIProvider" in their constructor
    - Neither file contains any import from '../infrastructure', '../ui', or '../cli'
    - AnalyzePost.test.ts has at least 3 test cases (success, passthrough return, error propagation)
    - GenerateCaption.test.ts has at least 3 test cases
    - `pnpm vitest run src/application/` exits 0 (all tests pass)
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>

  <done>AnalyzePost and GenerateCaption exist with constructor injection; tests verify delegation without any provider knowledge; typecheck and lint pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold AuditProfile use case</name>
  <files>src/application/AuditProfile.ts</files>

  <read_first>
    - src/application/AnalyzePost.ts — (just created) the exact pattern to replicate
    - src/domain/ports/AIProvider.ts — auditProfile(profileYaml: string): Promise<AuditResult> method signature
    - src/domain/entities/AuditResult.ts — the return type
    - .planning/phases/01-foundation/01-PATTERNS.md — AuditProfile.ts section (scaffold only, no analog)
    - .planning/REQUIREMENTS.md — AGNT-03 definition
  </read_first>

  <action>
  Create src/application/AuditProfile.ts as a Phase 1 scaffold.

  The class must follow the identical constructor injection pattern as AnalyzePost and GenerateCaption:
    - Import type AIProvider from '../domain/ports/AIProvider'
    - Import type AuditResult from '../domain/entities/AuditResult'
    - Export class AuditProfile
    - Constructor: constructor(private readonly provider: AIProvider) {}
    - Method: async execute(profileYaml: string): Promise<AuditResult> { return this.provider.auditProfile(profileYaml) }

  Add a file-level comment explaining this is a Phase 1 scaffold:
    // AuditProfile use case — Phase 1 scaffold.
    // The AIProvider.auditProfile() implementation is in Phase 2 (PROF-01/PROF-02/PROF-03).
    // The use case class is complete; only the provider body is deferred.

  No test file needed for this task — the scaffold has no logic to test; the provider throws
  'AuditProfile not implemented — Phase 2'. The AnalyzePost/GenerateCaption test patterns cover the delegation pattern already.

  CRITICAL import rule: only imports from '../domain/'. No infrastructure imports.
  </action>

  <verify>
    <automated>cd /Users/tcotd/seb-lab/instagram-advisor && pnpm typecheck && pnpm lint 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - src/application/AuditProfile.ts exists
    - AuditProfile.ts exports class AuditProfile with constructor(private readonly provider: AIProvider) {}
    - AuditProfile.ts has execute(profileYaml: string): Promise<AuditResult> method
    - AuditProfile.ts contains the Phase 1 scaffold comment referencing Phase 2
    - AuditProfile.ts has no imports from infrastructure/, ui/, or cli/
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>

  <done>AuditProfile is scaffolded with the correct constructor injection pattern and Phase 2 comment; typecheck and lint pass; architecture is complete for all three use cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/UI → Application | Presentation layers pass request objects; use cases delegate to provider |
| Application → Domain port | Use cases call provider.analyzePost() etc.; no direct provider knowledge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Application layer importing infrastructure directly | mitigate | eslint-plugin-boundaries disallows application → infrastructure import; enforced at lint time; test file verifies with grep |
| T-04-02 | Denial of Service | Use case swallowing provider Errors | mitigate | execute() methods do not catch Errors from provider; errors propagate to CLI (process.exit) or UI (setError); test verifies error propagation |
| T-04-SC | Tampering | npm/pnpm installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
After both tasks complete:

1. `pnpm vitest run src/application/` exits 0 — all use case tests pass
2. `pnpm typecheck` exits 0
3. `pnpm lint` exits 0
4. `grep -r 'from.*infrastructure\|from.*ui\|from.*cli' src/application/` returns empty
5. `ls src/application/` shows AnalyzePost.ts, GenerateCaption.ts, AuditProfile.ts (plus test files)
</verification>

<success_criteria>
- Three application use cases exist: AnalyzePost, GenerateCaption, AuditProfile (AGNT-01, AGNT-02, AGNT-03)
- All three use constructor injection of AIProvider — zero provider class imports in application layer
- AnalyzePost and GenerateCaption have passing unit tests
- AuditProfile is scaffolded with the correct shape for Phase 2 to implement
- typecheck and lint pass
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
